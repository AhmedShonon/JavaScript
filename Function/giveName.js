function giveName() {
  const x = "Shonon";
  return x;
}

// giveName();

const myName = giveName(); //without storing the return value of the function in the variable we can't show the return x or shonon or name
console.log(myName);
